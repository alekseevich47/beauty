# Staff API base (empty = same-origin / Vite proxy)
VITE_API_BASE_URL=

# Force demo mode without calling the API
VITE_DEMO_MODE=true

# Dev proxy target for /api
VITE_PROXY_TARGET=http://localhost:3001
